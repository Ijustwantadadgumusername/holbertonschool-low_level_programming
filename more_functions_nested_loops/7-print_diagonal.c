#include "main.h"
/**
 * print_diagonal - take a guess
 * @n: integer used
 * Return: returns 0
 */
void print_diagonal(int n)
{
	int i = 0;
	int s;

	for (i = 0; i < n; i++)
	{
		for (s = 0; s < i; s++)
			_putchar(' ');
		_putchar('\')
		_putchat('\n')
	}
	if (n <= 0)
		_putchar('\n')
}
