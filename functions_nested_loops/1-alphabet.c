#include "main.h"

/**
 * main - check the code
 * Return: Always 0.
 */
int main(void)
{
	char alphabet = 'a';

	while (alphabet <= 'z')
	{
		putchar(aplhabet);
		alphabet++;
	}
	putchar('\n');
	return (0);
}
