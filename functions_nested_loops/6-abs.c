#include "main.h"

/**
 * _abs - absolute value of an interger
 * @i: i think i need one of these so I chose i for Isaiah
 * Return: return 0
 */
int _abs(int i)
{
	if (i > 0)
		return (i);
	else if (i < 0)
		return (-i);
	else
		return (0);
}
