#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * _strlen - string length
 * @s: integer used
 * Return: return value
 */
int _strlen(char *s)
{
	int u = 33;


	strlen(u);

	return (u);

}
